Вот аккуратный и понятный **README.md** для твоего проекта на Next.js:

---

# 🗺️ Генератор маршрута

Веб-приложение на **Next.js 16**, позволяющее генерировать и сравнивать маршруты, выбирать события, настраивать параметры и визуально отображать результаты.
Проект использует современные UI-компоненты (Radix UI, shadcn UI), Tailwind CSS 4, а также различные удобные библиотеки для работы с формами, валидацией и визуализацией.

---

## 🚀 Технологии

**Основной стек:**

* **Next.js 16**
* **React 18**
* **TypeScript**
* **Tailwind CSS 4**
* **Radix UI**
* **shadcn/ui компоненты**
* **react-hook-form**
* **zod**
* **Recharts**

**Инструменты разработки:**

* TypeScript
* ESLint
* PostCSS
* Tailwind Merge
* Embla Carousel
* React Day Picker
* Date-fns

---

## 📦 Установка и запуск

### 1. Клонировать репозиторий

```bash
git clone https://github.com/<username>/<repo>.git
cd <repo>
```

### 2. Установить зависимости

Используя **npm**, **yarn** или **pnpm**:

```bash
npm install
# или
yarn install
# или
pnpm install
```

### 3. Запустить локально

```bash
npm run dev
```

Приложение будет доступно по адресу:

```
http://localhost:3000
```

---

## 🏗️ Скрипты

| Команда         | Описание                     |
| --------------- | ---------------------------- |
| `npm run dev`   | Запуск development-сервера   |
| `npm run build` | Сборка production-версии     |
| `npm run start` | Запуск собранного приложения |
| `npm run lint`  | Проверка кода ESLint         |

---

## 📁 Структура проекта (кратко)

```
app/
  page.tsx         — главная страница
  layout.tsx       — корневой layout
  globals.css      — глобальные стили
components/
  ...              — UI и функциональные компоненты
public/
  ...              — статичные файлы
styles/
  globals.css      — глобальные CSS
lib/
  utils.ts         — вспомогательные функции
hooks/
  ...              — кастомные React-хуки
```

---

## ⚙️ Конфигурации

Папка `config` включает:

* `next.config.mjs` — настройки Next.js
* `postcss.config.mjs` — PostCSS
* `tsconfig.json` — TypeScript

Tailwind CSS конфиг автоматически подключён через Next.js 13+.

---

## 🧩 Используемые библиотеки

### UI / анимации

* Radix UI
* Tailwind CSS
* shadcn/ui
* Vaul
* Sonner

### Формы и валидация

* react-hook-form
* zod

### Даты

* date-fns
* react-day-picker

### Графики

* Recharts

---

## 📤 Деплой

Рекомендуется деплой на **Vercel** (официальный хостинг Next.js):

1. Создать аккаунт на vercel.com
2. Подключить GitHub-репозиторий
3. Нажать **Deploy**

Vercel автоматически соберёт приложение.

---

## 📝 Лицензия

Проект доступен под свободной лицензией (укажи, какой именно — MIT, GPL или другую).

---

Если хочешь — могу адаптировать README под твой стиль, добавить скриншоты, GIF-анимации или секцию FAQ.
